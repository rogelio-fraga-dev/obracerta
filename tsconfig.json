{
  "//": "Root tsconfig for editor convenience. Each workspace has its own tsconfig that extends @obracerta/config.",
  "extends": "@obracerta/config/tsconfig/base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "exclude": ["node_modules", "dist", ".next", ".turbo"]
}
