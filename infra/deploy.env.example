# Config do scripts/deploy-cloud.sh — copie para infra/deploy.env (NÃO versionado).
# infra/deploy.env está no .gitignore.

# IP público (ou DNS) da EC2.
DEPLOY_HOST=54.207.10.20
# Caminho do .pem da instância (na sua máquina).
DEPLOY_KEY=/c/Users/rogelio/keys/obracerta-key.pem

# Opcionais (valores padrão mostrados):
# DEPLOY_USER=ec2-user
# DEPLOY_DIR=~/obracerta
# DEPLOY_BRANCH=master
